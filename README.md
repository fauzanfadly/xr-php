# xr-php
xr-php for PHP Version Manager.

pip install argparse
pip install subprocess
pip install pyinstaller

pyinstaller --onefile xr-php.py

then add to environment system :
<Path to this project>\xr-php\dist
